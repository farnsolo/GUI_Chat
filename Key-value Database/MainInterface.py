from Backend import Dict
def getName(name):
    # Return name from given IP
    return Dict[name]
    
def setName(key, value):
    # set name for ip
    # create new key-value pair
    Dict[key] = value